/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['zyhltmxgodkjesgtmjix.supabase.co', 'images.unsplash.com']
  }
};

export default nextConfig;
